name: My First WorkFlow
on: push
jobs: 
    first_job:
        runs-on: ubuntu-latest

        steps:
        - name: Welcome message
          run: echo "Welcome to Github Actions"
        - name: List the files
          run: ls
        - name: read the file
          run:  cat README.md